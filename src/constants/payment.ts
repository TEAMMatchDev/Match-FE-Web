// @ts-ignore
enum Currency {
    CURRENCY_KRW = "CURRENCY_KRW",
    // 다른 화폐 타입들을 추가할 수 있음
}

enum PgProvider {
    PG_PROVIDER_TOSSPAYMENTS = "PG_PROVIDER_TOSSPAYMENTS",
    // 다른 PG 프로바이더들을 추가할 수 있음
}

enum PayMethod {
    PAY_METHOD = "CARD",
}