import React from "react";

const PaymentRedirectScreen: React.FC = () => {

    const serverAuth = () => {


    };


    return (
        <>
            <button onClick={() => serverAuth()}>Redirect</button>
        </>
    );
};

export default PaymentRedirectScreen;
